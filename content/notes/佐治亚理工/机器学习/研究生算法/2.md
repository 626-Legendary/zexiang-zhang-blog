# 第一篇｜为什么我选择用 Next.js 写博客

> 技术选型本身，就是一次思考能力的展示。

## 背景

很多人写博客，只关心「能不能跑起来」。
但当我真正开始搭建自己的技术博客时，我意识到：**博客本身，就是一个长期维护的软件项目**。

这意味着：

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/*
=========================
Soft Minimal Theme (Comfortable Light/Dark)
=========================
*/

@layer base {
  :root {
    /* Backgrounds (更柔和的白，不刺眼) */
    --background: 210 25% 98%;        /* ~ #f8fafc */
    --foreground: 222 47% 11%;        /* ~ #0f172a */

    /* Surfaces */
    --card: 0 0% 100%;                /* #ffffff */
    --card-foreground: 222 47% 11%;

    --popover: 0 0% 100%;
    --popover-foreground: 222 47% 11%;

    /* Brand / Action (主按钮不那么“黑”，更高级耐看) */
    --primary: 222 35% 18%;           /* ~ #1f2937 */
    --primary-foreground: 210 40% 98%;/* ~ #f8fafc */

    /* Secondary surfaces (分层更明显) */
    --secondary: 210 20% 96%;         /* ~ #f1f5f9 */
    --secondary-foreground: 222 35% 18%;

    /* Muted (文本灰更舒服，减少灰糊糊感) */
    --muted: 210 20% 96%;
    --muted-foreground: 215 16% 42%;  /* ~ #64748b */

    /* Accent (用于 hover/选中态的底色) */
    --accent: 210 20% 94%;            /* ~ #eaf0f7 */
    --accent-foreground: 222 35% 18%;

    /* States */
    --destructive: 0 75% 55%;         /* ~ #ef4444 */
    --destructive-foreground: 210 40% 98%;

    /* Borders & focus (边框更干净，ring更柔) */
    --border: 214 20% 90%;            /* ~ #e2e8f0 */
    --input: 214 20% 90%;
    --ring: 222 35% 18%;

    --radius: 0.5rem;
  }

  .dark {
    /* Backgrounds (别用纯黑，降低眩光) */
    --background: 222 47% 8%;         /* ~ #0b1220 */
    --foreground: 210 40% 96%;        /* ~ #e5e7eb */

    /* Surfaces (卡片略亮于背景，层级清晰) */
    --card: 222 35% 12%;              /* ~ #111827 */
    --card-foreground: 210 40% 96%;

    --popover: 222 35% 12%;
    --popover-foreground: 210 40% 96%;

    /* Brand / Action (暗色里主按钮用浅色更自然) */
    --primary: 210 40% 96%;           /* ~ #e5e7eb */
    --primary-foreground: 222 47% 10%;/* ~ #0f172a */

    /* Secondary surfaces */
    --secondary: 217 26% 16%;         /* ~ #1f2937 */
    --secondary-foreground: 210 40% 96%;

    /* Muted */
    --muted: 217 26% 16%;
    --muted-foreground: 215 20% 70%;  /* ~ #94a3b8 */

    /* Accent */
    --accent: 217 26% 18%;            /* ~ #243244 */
    --accent-foreground: 210 40% 96%;

    /* States (暗色下 destructive 不要太暗也不要发紫) */
    --destructive: 0 62% 45%;         /* ~ #dc2626 */
    --destructive-foreground: 210 40% 96%;

    /* Borders & focus (边框更低对比，ring更清晰) */
    --border: 217 26% 18%;            /* ~ #243244 */
    --input: 217 26% 18%;
    --ring: 210 30% 85%;              /* 柔和高亮 */

    --radius: 0.5rem;
  }
}

@layer base {
  * {
    @apply border-border;
  }

  body {
    @apply bg-background text-foreground antialiased;
  }
}


/* ===== Markdown polish (minimal, pro) ===== */
.prose {
  --md-radius: 12px;
  --md-code-radius: 10px;
  --md-border: hsl(var(--border));
  --md-muted: hsl(var(--muted));
  --md-muted-foreground: hsl(var(--muted-foreground));
  --md-foreground: hsl(var(--foreground));
  --md-background: hsl(var(--background));
}

/* 标题：更像文档，间距更克制 */
.prose h1 {
  letter-spacing: -0.02em;
  margin-top: 0;
  margin-bottom: 0.9em;
}
.prose h2 {
  margin-top: 2.2em;
  margin-bottom: 0.8em;
  padding-bottom: 0.35em;
  border-bottom: 1px solid var(--md-border);
  letter-spacing: -0.01em;
}
.prose h3 {
  margin-top: 1.8em;
  margin-bottom: 0.6em;
  letter-spacing: -0.01em;
}
.prose h4 {
  margin-top: 1.4em;
  margin-bottom: 0.5em;
}

/* 让锚点跳转不会被顶部 sticky header 遮住（按需调大） */
.prose :is(h1, h2, h3, h4) {
  scroll-margin-top: 96px;
}

/* 段落与行高：更“阅读型” */
.prose p {
  line-height: 1.8;
  margin-top: 1em;
  margin-bottom: 1em;
}

/* 链接：更精致的下划线 + hover */
.prose a {
  text-decoration-thickness: 1px;
  text-underline-offset: 4px;
  text-decoration-color: color-mix(in hsl, var(--md-foreground) 35%, transparent);
}
.prose a:hover {
  text-decoration-color: color-mix(in hsl, var(--md-foreground) 70%, transparent);
}

/* 行内 code：Notion/GitHub 风格胶囊，别太吵 */
.prose :not(pre) > code {
  border: 1px solid var(--md-border);
  background: color-mix(in hsl, var(--md-muted) 70%, var(--md-background));
  border-radius: 0.5rem;
  padding: 0.15rem 0.4rem;
  font-size: 0.92em;
  color: color-mix(in hsl, var(--md-foreground) 92%, var(--md-muted-foreground));
}

/* 代码块：极简卡片感（暗色也好看） */
.prose pre {
  border: 1px solid var(--md-border);
  border-radius: var(--md-radius);
  padding: 1rem 1.1rem;
  overflow: auto;
  background: color-mix(in hsl, var(--md-muted) 55%, var(--md-background));
  box-shadow: 0 1px 0 rgba(0, 0, 0, 0.03);
}

/* 防止 code 再套背景 */
.prose pre code {
  background: transparent !important;
  padding: 0 !important;
}

/* 让长代码块滚动条更克制（可选） */
.prose pre::-webkit-scrollbar { height: 10px; }
.prose pre::-webkit-scrollbar-thumb {
  background: color-mix(in hsl, var(--md-border) 70%, transparent);
  border-radius: 999px;
}

/* 引用块：干净、细线、无大引号 */
.prose blockquote {
  border-left: 3px solid var(--md-border);
  padding-left: 1rem;
  margin-left: 0;
  margin-right: 0;
  color: color-mix(in hsl, var(--md-foreground) 78%, var(--md-muted-foreground));
}
.prose blockquote p:first-of-type::before,
.prose blockquote p:first-of-type::after {
  content: "";
}

/* 表格：GitHub-ish，支持横向滚动 + 圆角 */
.prose table {
  width: 100%;
  display: block;
  overflow-x: auto;
  border-collapse: separate;
  border-spacing: 0;
  border: 1px solid var(--md-border);
  border-radius: var(--md-radius);
}
.prose thead th {
  background: color-mix(in hsl, var(--md-muted) 75%, var(--md-background));
  color: var(--md-foreground);
  font-weight: 600;
}
.prose th,
.prose td {
  padding: 0.65rem 0.8rem;
  border-bottom: 1px solid var(--md-border);
  border-right: 1px solid var(--md-border);
  white-space: nowrap; /* 表格更像 GitHub，可横向滚 */
}
.prose tr:last-child td { border-bottom: 0; }
.prose th:last-child,
.prose td:last-child { border-right: 0; }
.prose tbody tr:hover td {
  background: color-mix(in hsl, var(--md-muted) 40%, transparent);
}

/* 图片：不顶满、圆角、轻边框 */
.prose img {
  border-radius: var(--md-radius);
  border: 1px solid var(--md-border);
}

/* 分割线：更细、更克制 */
.prose hr {
  border: 0;
  border-top: 1px solid var(--md-border);
  margin: 2rem 0;
}

/* 列表：更舒服的缩进与间距 */
.prose ul,
.prose ol {
  padding-left: 1.25rem;
}
.prose li {
  margin: 0.35rem 0;
}


```
* 可维护性比“快”重要
* 结构清晰比“炫”重要
* 可扩展性决定上限

## 为什么是 Next.js

我最终选择 Next.js，原因很简单：它在“工程化”和“表达内容”之间取得了很好的平衡。

* **文件即路由**：非常适合文章型内容
* **SSR / SSG 可选**：SEO 友好
* **生态成熟**：MD / MDX / RSS 都是现成方案

博客不是 demo，而是一个会陪你很多年的东西。

## 我的一个原则

> 技术博客，不是为了展示你会多少框架，
> 而是为了展示你如何做取舍。

---

# 第二篇｜技术博客里，哪些东西其实没必要？

> 做减法，比做加法难。

## 没必要的第一名：过度设计

* 复杂的动画
* 花哨的首页
* 信息密度过高的布局

这些在个人博客里，**几乎都是负收益**。

## 没必要的第二名：一开始就做全站系统

评论、点赞、用户系统、推荐算法……

你以为这是“专业”，但大多数情况下：

* 没人用
* 你也维护不了

**内容还没写几篇，就已经把自己拖垮了。**

## 真正该投入精力的地方

* 文章结构是否清晰
* 代码是否可复现
* 思路是否能被他人理解

博客的核心价值，永远是：

> **你在认真思考什么问题。**

---

# 第三篇｜写博客，其实是在训练工程思维

> 表达，是工程能力的一部分。

## 写作 ≠ 记录

好的技术文章，不是流水账，而是：

* 问题定义
* 约束条件
* 解决方案
* 权衡取舍

这和写系统设计文档，本质是一样的。

## 博客对工程师的隐性收益

* 强迫你把模糊的想法讲清楚
* 暴露你真正不懂的地方
* 形成可复用的思考模型

## 我的建议

如果你是工程师：

* 不要追求“更新频率”
* 追求**每一篇都值得几年后再看**

写博客不是输出内容，
而是在构建你自己的思维仓库。

---

> 如果哪天你回头看这些文章，发现依然成立，
> 那说明你真的进步了。
