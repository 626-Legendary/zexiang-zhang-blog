---
title: 操作系统原理 - 第四周测验
date: 2025-12-30
tags: [Operating Systems,学习笔记,答案]
cover: /covers/操作系统原理/北京大学/banner.png
---

## Question 1

下列哪一个调度算法的**开销比较小**？

* **先来先服务（FCFS）**
* 最高响应比优先（HRRN）
* 短作业优先（SJF）
* 多级反馈队列（Feedback）

---

## Question 2

下列哪一个调度算法**对短进程不利**？

* 时间片轮转（RR）
* 最高响应比优先（HRRN）
* **先来先服务（FCFS）**
* 短作业优先（SJF）

---

## Question 3

假设三个进程 P1、P2、P3 同时到达，
执行时间分别为 T1、T2、T3，且 T1 < T2 < T3。

采用 **短作业优先（SJF）** 调度算法时，
其平均周转时间为：

* **(3T1 + 2T2 + T3) / 3**
* T1 + T2 + T3
* 1/T1 + 1/T2 + 1/T3
* (T1 + T2 + T3) / 3

---

## Question 4

设有四个进程，其到达时刻和处理时间如下：

| 进程 | 到达时刻 | 处理时间 |
| -- | ---- | ---- |
| P1 | 0    | 50   |
| P2 | 10   | 30   |
| P3 | 30   | 10   |
| P4 | 50   | 10   |

在 **时刻 50**，采用 **最高响应比优先（HRRN）** 调度算法，
被选中的进程是：

* P1
* P4
* P2
* **P3**

---

## Question 5

有三个进程 P1、P2、P3，运行时间均为 50ms。
时间片大小为 10ms，不考虑上下文切换开销。

采用 **时间片轮转（RR）** 算法，
执行完这三个进程的**平均完成时间**是：

* 50ms
* 150ms
* **140ms**
* 100ms

---

## Question 6

设有四个进程，其到达时刻和处理时间如下：

| 进程 | 到达时刻 | 处理时间 |
| -- | ---- | ---- |
| P1 | 0    | 8    |
| P2 | 3    | 6    |
| P3 | 5    | 2    |
| P4 | 6    | 5    |

采用 **最短剩余时间优先（SRTN）** 调度算法，
其调度顺序是：

* P1、P3、P4、P2
* P1、P2、P1、P3、P4
* **P1、P3、P1、P4、P2**
* P3、P4、P2、P1

---

## Question 7

采用下列哪一个调度算法会产生**饥饿（Starvation）**现象？

* **多级反馈队列（Feedback）**
* 最高响应比优先（HRRN）
* 时间片轮转（RR）
* 先来先服务（FCFS）

---

## Question 8

下列关于**多级反馈队列（Feedback）调度算法**的叙述中，
哪一个是不正确的？

* 它是 BSD 5.3 所采用的调度算法
* **它对计算密集型（CPU 型）进程更友好**
* 它为不同就绪队列分配不同时间片
* 它是一种综合调度算法

---

## Question 9

在 Windows 线程调度算法中，为解决饥饿、改善吞吐量和响应时间，
会临时提升部分线程的优先级。

下列哪一类线程 **不会被提升优先级**？

* I/O 结束后被唤醒的线程
* **运行完时间配额的线程**
* 在就绪队列中等待超时的线程
* 由于窗口活动被唤醒的线程

---

## Question 10（多选）

假设系统只有一个就绪队列，下列哪些事件**一定会导致进程切换**？

* 创建一个进程
* **进程时间片到期**
* **进程等待 I/O**
* **进程执行中发生终止（abort）异常**
* 从等待队列唤醒一个进程

---

## Question 11（多选）

下列关于**进程优先级与优先数**的叙述中，哪些是正确的？

* 优先级一旦确定就不再改变
* **系统进程通常比用户进程优先级高**
* **优先数是一个数值**
* **进程优先数决定进程的优先级**
* 优先数大的进程优先级一定高

---

## Question 12（判断）

时间片长度过小会增加系统开销。

* **对**
* 错

---

## Question 13（判断）

一个比正在运行进程优先级更高的进程进入就绪态，
**一定**会引起进程切换。

* 对
* **错**

---

## Question 14（判断）

非抢占式最高优先级调度算法会导致优先级反转问题。

* 对
* **错**

---

## Question 15（判断）

进程在不同 CPU 之间迁移，会导致高速缓存和 TLB 失效。

* 错
* **对**