---
title: 操作系统原理 - 第十周测验
date: 2025-12-30
tags: [Operating Systems,学习笔记,答案]
cover: /covers/操作系统原理/北京大学/banner.png
---

## Question 1

下列关于 FAT 文件系统的叙述中，哪一个是不正确的？

* **FAT16 文件系统支持 Unicode 编码**
* 文件分配表 FAT 的作用之一是空闲簇的管理
* FAT 文件系统中，目录项即是文件控制块
* FAT32 中每一个文件对应两个或两个以上的目录项

---

## Question 2

系统为了管理文件，设置了专门的数据结构文件控制块（FCB）。
FCB 是在执行哪一个系统调用时建立的？

* seek
* copy
* open
* **create**

---

## Question 3

使用文件前要先打开文件。在执行完“打开”文件系统调用后，系统会返回给进程一个：

* 文件打开方式
* 内存地址
* 文件长度
* **文件描述符**

---

## Question 4

下列哪一项**不是**打开文件时所做的工作？

* 检查操作的合法性
* 填写用户打开文件表中的读写指针
* **填写文件控制块中的文件读写方式**
* 检查文件名所对应的文件控制块是否已调入内存

---

## Question 5

下列关于磁盘结构及磁盘调度的叙述中，哪一条是不正确的？

* 磁盘调度时，首先是移臂调度，然后是旋转调度
* **若读的扇区在磁盘最里道，则磁盘调度的时间最长**
* 磁盘的物理块号与磁盘地址可以相互转换
* 最短寻道时间优先（SSTF）调度算法可能导致饥饿现象

---

## Question 6

假设磁头当前在 65 号柱面，待访问柱面号为：
85、46、114、16、116。

若采用 **最短寻道时间优先（SSTF）** 调度算法，
磁头需要移动的柱面数是：

* 181
* 159
* **149**
* 139

---

## Question 7

下列哪一种磁盘调度算法可能引起磁头臂频繁**大幅度移动**？

* 最短寻道时间优先算法
* **先来先服务算法**
* 扫描算法
* 旋转调度算法

---

## Question 8

有 4 个访问第 66 柱面的访盘请求，其访问要求如下：

| 请求号 | 柱面号 | 磁头号 | 扇区号 |
| --- | --- | --- | --- |
| ①   | 66  | 1   | 4   |
| ②   | 66  | 4   | 2   |
| ③   | 66  | 4   | 4   |
| ④   | 66  | 2   | 7   |

下列哪一种执行顺序可以获得**最小的平均服务时间**？

* **② → ① → ④ → ③**
* ① → ② → ③ → ④
* ① → ② → ④ → ③
* ② → ① → ③ → ④

---

## Question 9

RAID 技术是通过下列哪一种方法来提高文件系统性能的？

* 镜像
* **数据分条**
* 海明码校验
* 奇偶校验

---

## Question 10（多选）

设计文件系统时应尽量减少访问磁盘的次数，以提高文件系统性能。
下列哪些措施可以减少磁盘服务时间？

* **当前目录**
* **磁盘的旋转调度**
* **块高速缓存**
* **磁盘碎片整理**
* 内存映射文件

---

## Question 11（多选）

在 UNIX 文件系统中，若文件 F 的权限是 **765**，则表示：

* **文件的其他用户可以运行该文件**
* **文件的属主可以运行该文件**
* 文件的其他用户可以读写该文件
* **文件的同组用户可以读写该文件**
* **文件的属主可以读写该文件**

---

## Question 12（判断）

文件系统的一致性是指文件卷在磁盘上的管理数据是正确的。

* **对**
* 错

---

## Question 13（判断）

在 UNIX 文件系统中，将 i 节点均匀分布在磁盘上，并在建立文件时将其 i 节点放在靠近数据块的位置，可以提高文件系统性能。

* **对**
* 错

---

## Question 14（判断）

若采用块高速缓存机制，读文件数据块时，若缓存中不存在该块，则直接将数据块读入进程地址空间。

* 对
* **错**

---

## Question 15（判断）

若采用块高速缓存机制，进程在运行时打开文件并读入部分内容后，
整个系统中会有 **三处存储空间** 包含该文件信息。

* **对**
* 错
