---
title: 操作系统原理 - 第八周测验
date: 2025-12-30
tags: [Operating Systems,学习笔记,答案]
cover: /covers/操作系统原理/北京大学/banner.png
---

## Question 1

提出存储层次体系的主要依据是：

* 多道程序设计技术
* **程序访问的局部性原理**
* 虚拟存储技术
* 存储保护技术

---

## Question 2

虚拟页式存储系统中，页表项中用于标记页面**尚未读入内存**的是：

* 访问位
* 禁止缓存位
* 保护位
* **有效位**

---

## Question 3

在虚拟页式存储系统的页面置换算法中，LRU 算法是指：

* 先置换最早进入内存的页面
* 先置换近期内被访问次数最少的页面
* 先置换以后不会使用的页面
* **先置换近期最长时间没有被访问的页面**

---

## Question 4

在虚拟页式存储系统中，若页面大小为 4KB，页表项大小为 4 字节，
采用三级页表结构可表示的虚拟地址空间大小是：

* 2⁴⁸
* 2²⁰
* **2⁴²**
* 2³⁹

---

## Question 5

在虚拟页式存储系统中，**对缺页异常没有影响**的因素是：

* 分配给进程的页框数目
* **页表在内存中的位置**
* 页面置换算法
* 程序本身的编制方法

---

## Question 6

根据下列哪一因素引入了**工作集模型**？

* 页面置换算法选择不合理
* **程序的局部性原理**
* 分配给进程的页框数目太少
* 系统效率下降

---

## Question 7

下列关于工作集模型的叙述中，哪一个是错误的？

* **工作集大小与缺页率无关**
* 每个进程有一个工作集
* 工作集的大小是可以调整的
* 工作集模型可以解决系统的颠簸（抖动）问题

---

## Question 8

某虚拟页式存储系统为进程分配 3 个页框（初始为空），
页面访问序列为：

```
4，3，2，1，4，3，5，4，3，2，1，5
```

若采用 **OPT（最佳）页面置换算法**，缺页次数为：

* 10
* 8
* 9
* **7**

---

## Question 9

某进程将一个 `128 × 256` 的整型数组初始化为 0，
页面大小为每页 256 个整数，数组按**行存放**，
系统分配 1 个页框，采用 FIFO 页面置换算法。

缺页异常次数为：

* 64 × 128
* **128 × 256**
* 64 × 256
* 256 × 256

---

## Question 10

下列存储管理技术中，哪一项**不需要硬件支持**？

* 快表（TLB）
* 存储保护
* 地址转换
* **交换技术**

---

## Question 11

下列哪一项**不属于**页错误（Page Fault）的发生原因？

* 所访问的页面在磁盘上
* 页表项的 U/S 位为 S
* 虚拟地址落在无内容区域
* **用户进程对页面执行读操作**

---

## Question 12

在引入快表（TLB）后，MMU 的正确工作顺序中，
下列哪一项是**不正确的顺序**？

* ② → ① → ③
* ② → ③
* ② → ① → ④ → ③
* **② → ④ → ① → ③**

---

## Question 13（多选）

下列哪些因素影响了虚拟存储器的容量？

* 物理内存大小
* 数据存放的实际地址
* **磁盘空间大小**
* 快表大小
* **计算机系统的寻址机制**

---

## Question 14（多选）

下列关于快表（TLB）的叙述中，哪些是正确的？

* **快表查找是按内容并行完成的**
* **引入快表可以加快地址转换速度**
* **进程切换时操作系统会刷新 TLB**
* **快表内容是页表的子集**
* 快表保存在内存固定位置

---

## Question 15（判断）

虚拟存储器是构建在存储体系之上的，是操作系统分配给进程的地址空间。

* **对**
* 错

---

## Question 16（判断）

在虚拟页式存储系统中，选择页面置换算法时应尽量减少或避免颠簸（抖动）现象。

* 错
* **对**

---

## Question 17（判断）

当内存空闲页框不足时，操作系统通过**清除策略**收回正在运行进程的部分页框。

* 错
* **对**

---

## Question 18（判断）

Linux 中为加快进程创建采用了**写时复制（COW）技术**，该技术由存储管理模块支持。

* 错
* **对**
