---
title: 操作系统原理 - 期末考试
date: 2025-12-30
tags: [Operating Systems,学习笔记,答案]
cover: /covers/操作系统原理/北京大学/banner.png
---

## Question 1（多选）

下列关于操作系统的叙述中，哪些是正确的？

* **分时操作系统是典型的交互式系统**
* **用户可以通过系统调用向操作系统提出服务请求**
* **在汽车电子系统中使用的操作系统属于嵌入式操作系统**
* **操作系统管理着计算机系统中的各种资源**
* 操作系统实现时要求用户考虑硬件操作细节

---

## Question 2

下列关于各种不同类型操作系统的叙述中，哪一个是错误的？

* 分布式操作系统支持系统中若干台计算机相互协作完成同一任务
* **设计实时系统时首先要考虑的是如何提高系统的资源利用率**
* 批处理系统可以采用 SPOOLing 技术实现
* 嵌入式操作系统和实时操作系统都追求高可靠性

---

## Question 3

下列关于中断和异常的叙述中，哪一个是错误的？

* 启动 I/O 指令只能在内核态下运行
* **Page Fault 属于 I/O 中断**
* x86 系列处理器提供的 4 个处理器特权级别中 R0 的特权级别最高
* 中断向量（中断描述符）保存了程序状态字和中断入口程序地址

---

## Question 4

下列关于系统调用的叙述中，哪一个是错误的？

* 系统调用时调用程序位于用户态，被调用程序位于内核态
* 系统调用的功能是由内核函数实现的
* 打开磁盘上的一个文件属于系统调用
* **执行系统调用时，不能利用通用寄存器完成参数传递**

---

## Question 5（多选）

下列各种事件中，会导致进程从运行态变为等待态的事件是？

* 被高优先级进程抢占
* **申请内存失败**
* 运行进程的时间片用完
* **启动 I/O 设备**
* **进程执行 P 操作**

---

## Question 6

在 UNIX 操作系统中运行如下 C 语言程序，假设程序正确执行，则运行结果是：

* **father a=1，child a=2，child's child a=2**
* father a=2，child a=3，child's child a=4
* father a=2，child a=3，child's child a=2
* father a=2，child a=2，child's child a=2

---

## Question 7

下列关于进程调度算法的叙述中，哪一个是错误的？

* 抢占式最高优先级调度算法会导致优先级反转问题
* 多级反馈队列调度算法会使 CPU 型进程产生“饥饿”现象
* **最高响应比优先（HRRN）调度算法实现开销小**
* 先来先服务（FCFS）调度算法对长进程后的短进程不利

---

## Question 8

采用时间片轮转调度算法（时间片为 1），P5 第一次上 CPU 的时间是：

* 8
* **10**
* 7
* 9

---

## Question 9

下列关于进程互斥解决方案的叙述中，哪一个是错误的？

* 采用 Peterson 算法会出现“忙等待”
* TSL 指令适用于多处理器
* 自旋锁适用于多处理器
* **Peterson 算法采用了原子操作**

---

## Question 10

下列关于管程的叙述中，哪一个是错误的？

* 管程提供条件变量和 wait/signal 操作
* 应用 MESA 管程时条件检测至少需要两次
* **HOARE 管程让先进入管程的进程等待**
* 管程的互斥性由编译器保证

---

## Question 11（多选）

下列关于存储管理的叙述中，哪些是正确的？

* **MMU 完成逻辑地址到物理地址的转换**
* 紧缩技术可以解决内碎片问题
* **动态地址重定位在进程执行过程中完成**
* **页表起始地址保存在 PCB 中**
* 对 TLB 的查找是顺序比较完成的

---

## Question 12

根据 LRU 页面置换算法，应被淘汰的页号是：

* 9
* **4**
* 3
* 5

---

## Question 13

下列关于文件系统的叙述中，哪一个是错误的？

* **RAID 镜像方式可以提高文件系统性能**
* 打开文件后 i 节点会读入内存
* 块高速缓存可以提高性能
* FAT 文件系统中文件下一簇号存放在 FAT 表中

---

## Question 14

UNIX 系统中创建普通文件时，操作系统不可能执行的是：

* 分配文件内容磁盘块
* 创建 i 节点
* **创建文件所在的目录文件**
* 创建目录项

---

## Question 15

下列关于虚设备技术的叙述中，哪一个是错误的？

* 引入虚设备技术是为了提高设备利用率
* **用低速设备模拟高速设备**
* SPOOLing 是典型虚设备技术
* 虚设备是在一类设备上模拟另一类设备

---

## Question 16（多选）

下列关于设备管理的叙述中，哪些是正确的？

* **缓冲技术可以提高 I/O 性能**
* 设备驱动程序属于设备无关软件层
* **SPOOLing 是用磁盘空间换取时间**
* I/O 独立编址下驱动可用 C 语言编写
* **操作系统屏蔽具体 I/O 操作细节**

---

## Question 17

下列关于银行家算法的叙述中，哪一个是正确的？

* 不安全状态一定死锁
* **安全状态一定无死锁**
* 银行家算法用于解除死锁
* 银行家算法破坏请求和保持条件

---

## Question 18（多选）

关于死锁避免与检测的叙述中，哪些是正确的？

* **S1 在最后一只筷子分配上更严格**
* S1 会限制资源申请顺序
* **S2 允许哲学家先拿右边筷子**
* **S1 不允许进入不安全状态**
* **S1 需要事先给出最大需求**

---

## Question 19

采用一级页表时，该进程页表大小为：

* **4MB**
* 32MB
* 32KB
* 4KB

---

## Question 20（填空）

采用二级页表，两级页表至少占据：
**8 KB**

---

## Question 21（填空）

一级页表大小是二级页表 4 倍时，至少占据：
**12 KB**

---

## Question 22

采用 NRU 页面置换算法将被置换的页面是：

* 0
* 3
* 2
* 1

---

## Question 23

关于文件拷贝操作的正确结论是：

* **① 比 ② 时间长，且差异明显**
* ② 比 ① 长，差异不明显
* ① 比 ② 长，差异不明显
* ② 比 ① 长，且差异明显

---

## Question 24（填空）

最大支持文件块数为：
**65620**

---

## Question 25

最大文件大小是：

* 32MB + 68KB
* 16MB + 40KB
* **64MB + 268KB**
* 128MB + 536KB

---

## Question 26

至少需要的磁盘读取次数是：

* **15 + 2×2⁸ + 2¹⁶**
* 14 + 2×2⁸ + 2¹⁶
* 16 + 4×2⁸ + 2×2¹⁶
* 13 + 4×2⁸ + 2×2¹⁶

---

## Question 27

最大文件大小是：

* 24KB + 1MB + 1GB + 1TB
* 16KB + 1MB + 1GB + 1TB
* 64KB + 4MB + 4GB + 4TB
* **48KB + 4MB + 4GB + 4TB**

---

## Question 28（判断）

是否会进入该状态：
**是**

---

## Question 29（多选）

正确的术语关联是：

* ①Ⅰ
* **②Ⅱ**
* **③Ⅰ**
* ③Ⅱ
* ②Ⅲ
* **①Ⅲ**

---

## Question 30（填空）

三个信号量的初值是：
**100**

---

## Question 31

代码中②对应的操作是：

* P(plate)
* V(plate)
* P(apple)
* **V(apple)**
* P(peach)
* V(peach)

---
